import ContactClient from "@/components/ContactClient";

export const metadata = {
  title: "Get Your Free Growth Audit — Performance Report",
  description: "Claim your free local business digital audit today. We check your mobile speeds, search positions, Google Map listings, and ad setup opportunity gaps.",
};

export default function ContactPage() {
  return <ContactClient />;
}
